#pragma once

/**
 * LoDB Diagnostics
 *
 * Comprehensive test suite for LoDB that exercises multiple databases,
 * tables, and all CRUD operations (inserts, gets, updates, deletes,
 * selects, counts).
 */
void lodb_diagnostics();
