變更紀錄
2025/09/29
src/modules/HermesXInterfaceModule.cpp: 為 registerRawButtonPress() 加入詳細 Debug 訊息；調整 setEmergencyActive() 成功與失敗時的 log；移除不必要的 toUs 變數，同步過濾 routing loopback；強化 handleExternalNotification() 與 routing 過濾流程。
src/modules/HermesXInterfaceModule.h: 將 HermesButtonSource 與 instance 等成員改為 public；新增 SOS/SAFE 按鍵時使用的列舉與狀態欄位。
src/input/InputBroker.cpp: 新增 HermesX 支援，辨識 rotEnc1 壓鍵事件並呼叫 registerRawButtonPress()。
src/modules/HermesXInterfaceModule.cpp: 加入 Emergency 模組相關 WARN/INFO log，便於追查設定問題。
src/modules/CannedMessageModule.cpp: 修復虛擬鍵盤中 SHIFT/BKSP/ENTER 的描述文字，並消除編譯警告。
src/input/InputBroker.cpp: 增加 configuration.h 與 HermesX include，並引入 <cstring> 以支援上述邏輯。
目前 platformio run -e heltec-wireless-tracker 已可成功編譯，若要三連擊觸發 SOS，記得將 Emergency 模式設為非 OFF，白名單也要允許本節點。

2025/10/02
src/modules/CannedMessageModule.cpp: setActiveList() 在進入 EM/DRILL 清單時立即呼叫 requestFocus()，畫面會立刻切換到緊急頁面。
src/modules/HermesXInterfaceModule.cpp: 新增 handleAckNotification()，由 ReliableRouter 的 callback 統一處理 ACK/NACK，維持 waitingForAck 與 LED/表情狀態一致。
src/modules/HermesEmergencyUi.cpp,h: 提供 EM UI 動畫管理（setup/onSend/onReceive/onAck/currentFace），供 HermesXInterfaceModule 調用。
